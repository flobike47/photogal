name: Build and push images

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  PLATFORMS: linux/arm64
  API_IMAGE: photogal-api
  WEB_IMAGE: photogal-web

concurrency:
  group: build-and-push-${{ github.ref }}
  cancel-in-progress: false

jobs:
  # ---------------------------------------------------------------------------
  # API — Fastify + better-sqlite3 + sharp
  # ---------------------------------------------------------------------------
  build-api:
    name: Build & push API
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Short SHA
        id: vars
        run: echo "sha=$(echo ${GITHUB_SHA} | cut -c1-7)" >> $GITHUB_OUTPUT

      - name: Setup QEMU
        uses: docker/setup-qemu-action@v3
        with:
          platforms: arm64

      - name: Login to registry
        run: |
          echo "Registry host: ${{ secrets.REGISTRY_URL }}"
          echo "${{ secrets.REGISTRY_USERPASSWORD }}" | podman login \
            --username "${{ secrets.REGISTRY_USERNAME }}" \
            --password-stdin \
            "${{ secrets.REGISTRY_URL }}"

      - name: Build API image
        id: build-image
        uses: redhat-actions/buildah-build@v2
        with:
          image: ${{ env.API_IMAGE }}
          tags: latest sha-${{ steps.vars.outputs.sha }}
          platforms: ${{ env.PLATFORMS }}
          context: .
          containerfiles: |
            ./apps/api/Dockerfile

      - name: Push API to registry
        id: push-image
        uses: redhat-actions/push-to-registry@v2
        with:
          image: ${{ steps.build-image.outputs.image }}
          tags: ${{ steps.build-image.outputs.tags }}
          registry: ${{ secrets.REGISTRY_URL }}
          username: ${{ secrets.REGISTRY_USERNAME }}
          password: ${{ secrets.REGISTRY_USERPASSWORD }}

      - name: Print pushed API image
        run: echo "Pushed → ${{ steps.push-image.outputs.registry-paths }}"

      - name: Job summary API
        run: |
          {
            echo "## API"
            echo ""
            echo "| Tag | Image |"
            echo "| --- | --- |"
            echo "| \`latest\` | \`${{ env.API_IMAGE }}:latest\` |"
            echo "| \`sha-${{ steps.vars.outputs.sha }}\` | \`${{ env.API_IMAGE }}:sha-${{ steps.vars.outputs.sha }}\` |"
          } >> "$GITHUB_STEP_SUMMARY"

  # ---------------------------------------------------------------------------
  # Web — bundle Vite servi par nginx
  # VITE_GOOGLE_CLIENT_ID est injecté au build (variable publique côté client)
  # ---------------------------------------------------------------------------
  build-web:
    name: Build & push Web
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Short SHA
        id: vars
        run: echo "sha=$(echo ${GITHUB_SHA} | cut -c1-7)" >> $GITHUB_OUTPUT

      - name: Setup QEMU
        uses: docker/setup-qemu-action@v3
        with:
          platforms: arm64

      - name: Login to registry
        run: |
          echo "Registry host: ${{ secrets.REGISTRY_URL }}"
          echo "${{ secrets.REGISTRY_USERPASSWORD }}" | podman login \
            --username "${{ secrets.REGISTRY_USERNAME }}" \
            --password-stdin \
            "${{ secrets.REGISTRY_URL }}"

      - name: Build Web image
        id: build-image
        uses: redhat-actions/buildah-build@v2
        with:
          image: ${{ env.WEB_IMAGE }}
          tags: latest sha-${{ steps.vars.outputs.sha }}
          platforms: ${{ env.PLATFORMS }}
          context: .
          containerfiles: |
            ./apps/web/Dockerfile
          build-args: |
            VITE_GOOGLE_CLIENT_ID=${{ secrets.VITE_GOOGLE_CLIENT_ID }}

      - name: Push Web to registry
        id: push-image
        uses: redhat-actions/push-to-registry@v2
        with:
          image: ${{ steps.build-image.outputs.image }}
          tags: ${{ steps.build-image.outputs.tags }}
          registry: ${{ secrets.REGISTRY_URL }}
          username: ${{ secrets.REGISTRY_USERNAME }}
          password: ${{ secrets.REGISTRY_USERPASSWORD }}

      - name: Print pushed Web image
        run: echo "Pushed → ${{ steps.push-image.outputs.registry-paths }}"

      - name: Job summary Web
        run: |
          {
            echo "## Web"
            echo ""
            echo "| Tag | Image |"
            echo "| --- | --- |"
            echo "| \`latest\` | \`${{ env.WEB_IMAGE }}:latest\` |"
            echo "| \`sha-${{ steps.vars.outputs.sha }}\` | \`${{ env.WEB_IMAGE }}:sha-${{ steps.vars.outputs.sha }}\` |"
          } >> "$GITHUB_STEP_SUMMARY"

  # ---------------------------------------------------------------------------
  # Redéploiement Portainer une fois les deux images publiées
  # ---------------------------------------------------------------------------
  redeploy-portainer:
    name: Trigger Portainer redeploy
    runs-on: ubuntu-latest
    needs: [build-api, build-web]
    if: success()
    steps:
      - name: Call Portainer stack webhook
        env:
          WEBHOOK: ${{ secrets.PORTAINER_WEBHOOK_URL }}
        run: |
          if [ -z "$WEBHOOK" ]; then
            echo "PORTAINER_WEBHOOK_URL n'est pas configuré — skip redeploy."
            exit 0
          fi
          echo "Triggering Portainer redeploy..."
          curl -fsSL -X POST "$WEBHOOK" \
            -H 'Content-Length: 0' \
            --max-time 30
          echo "Portainer notifié — la stack va pull les nouvelles images."

      - name: Final summary
        if: always()
        run: |
          SHORT_SHA=$(echo ${{ github.sha }} | cut -c1-7)
          {
            echo "## Déploiement terminé"
            echo ""
            echo "**Commit :** [\`$SHORT_SHA\`](${{ github.server_url }}/${{ github.repository }}/commit/${{ github.sha }})"
            echo ""
            echo "### Images publiées"
            echo ""
            echo "| Service | Tags |"
            echo "| --- | --- |"
            echo "| API | \`${{ env.API_IMAGE }}:latest\` · \`${{ env.API_IMAGE }}:sha-$SHORT_SHA\` |"
            echo "| Web | \`${{ env.WEB_IMAGE }}:latest\` · \`${{ env.WEB_IMAGE }}:sha-$SHORT_SHA\` |"
            echo ""
            echo "### Liens"
            echo ""
            echo "- [Run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})"
            echo "- Portainer : webhook déclenché"
          } >> "$GITHUB_STEP_SUMMARY"

          echo ""
          echo "===================="
          echo "Images publiées :"
          echo "  ${{ env.API_IMAGE }}:latest"
          echo "  ${{ env.API_IMAGE }}:sha-$SHORT_SHA"
          echo "  ${{ env.WEB_IMAGE }}:latest"
          echo "  ${{ env.WEB_IMAGE }}:sha-$SHORT_SHA"
          echo "===================="
